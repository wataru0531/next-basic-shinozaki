

// dashboardページ

const DashboardPage = () => {
  
  // throw new Error()
  // → 最も近いエラーページが表示される
  // dashboardセグメントにerror.tsxがない場合はルートのerror.tsxをレンダリングする

  return(
    <h1>Dashboardページ</h1>
  )
}

export default DashboardPage;